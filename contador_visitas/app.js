// Declaramos una constante llamada contador
const contador = 100;  
// Aunque definimos contador, en este ejemplo no lo usamos directamente

// Sumamos 100 + 25 y guardamos el resultado en la variable 'aumenta'
let aumenta = 100 + 25;   // aumenta = 125

// Restamos 10 al valor de 'aumenta' y lo guardamos en 'error'
let error = aumenta - 10; // error = 125 - 10 = 115

// Asignamos el valor de 'error' a la variable 'total'
let total = error;        // total = 115

// Mostramos el resultado final en consola
console.log("El valor final del contador es:" + total);