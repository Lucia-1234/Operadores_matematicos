// Definimos las tres notas
let nota1 = 10;   // Primera nota
let nota2 = 50;   // Segunda nota
let nota3 = 20;   // Tercera nota

// Calculamos el promedio
// Fórmula: (nota1 + nota2 + nota3) / cantidad de notas
let promedio = (nota1 + nota2 + nota3) / 3;

// Mostramos el resultado en consola
console.log("El promedio de las tres notas es: " + promedio);